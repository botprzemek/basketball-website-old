// import { ref, set } from 'firebase/database';
// import { database } from '../Firebase.js';
//
// export const writeData = (userData, callback) => set(ref(database, `users/${userData.name}-${userData.lastname}`), userData).then(callback => { return callback });